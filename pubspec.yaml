name: split_conta
description: Aplicativo para divisão de contas com preloader

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  lottie: ^2.7.0
  shared_preferences: ^2.2.2
  custom_refresh_indicator: ^1.0.0
  rive: ^0.8.1
  animated_text_kit: ^4.2.2
  provider: ^6.0.5
  image_picker: ^0.8.7+5
  intl: ^0.18.1
  path_provider: ^2.0.15

flutter:
  uses-material-design: true
  assets:
    - assets/animations/